general:
  branches:
    only:
      - master
      - circleci

machine:
  pre:
   - pyenv global 3.5.1

test:
  pre:
    - which python
    - python --version
    - python bin/main.py

  override:
    - python -c 'print("ok")'
    - pip list
    - grep elasticache all/all.dic
    - python -m unitetest tests/test_main.py
