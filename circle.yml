general:
  branches:
    only:
      - master
      - circleci

machine:
  pre:
   - pyenv global 3.5.1

test:
  pre:
    - which python
    - python --version
    - python bin/main.py

  override:
    - python -c 'print("ok")'
    - pip list
    - grep elasticache all/all.dic
    - python -m unittest tests/test_main.py -v

notify:
  webhooks:
    # gitter
    - url: https://webhooks.gitter.im/e/09af529be321ba7db7c7
